ktor {
    security {
        ssl {
            keyStore = keystore.jks
            keyAlias = sampleAlias
            keyStorePassword = foobar
            privateKeyPassword = foobar
        }
    }
    deployment {
        sslPort = 8443
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.urepair.ApplicationKt.module ]
    }
}

ktor {
    deployment {
        port = 5000
        port = ${?PORT}
        sslPort = 8433
    }
    application {
        modules = [ com.urepair.ApplicationKt.module ]
    }

    security {
        ssl {
            keyStore = urepair_me.jks
        }
    }
}
